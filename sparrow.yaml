tasks:
  -
    name: example_task
    language: Bash
    default: true
    code: |
      echo "Hello Kris! how are you?"
    if:
      language: Raku
      code: |
        #say "skip this task ...";
        #update_state %( status => "skip")
